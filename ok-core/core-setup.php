<?php
/**
 * OK ძრავის ძირითადი ჰუკების და ფილტრების რეგისტრაცია.
 * ეს ფაილი ეშვება მას შემდეგ, რაც ყველა ფუნქცია და ობიექტი იქმნება.
 */

declare(strict_types=1);

if (!defined('OK_LOADED')) {
    exit('Access Denied.');
}

/**
 * --------------------------------------------------------------------------
 * Core Filters
 * --------------------------------------------------------------------------
 */

// რთავს shortcode-ების დამუშავებას პოსტის კონტენტში (განახლებული სახელი!)
add_ok_filter('the_content', 'do_shortcode', 11);

/**
 * --------------------------------------------------------------------------
 * Core Actions
 * --------------------------------------------------------------------------
 */

// შეტყობინებების სისტემის მთავარი ჰუკი (განახლებული სახელი!)
add_ok_action('ok_notification', 'ok_handle_notification', 10, 1);

// admin_menu ჰუკზე მიბმა (განახლებული სახელი!)
add_ok_action('admin_menu', 'ok_core_register_admin_pages');